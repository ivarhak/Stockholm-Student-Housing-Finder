requests>=2.31
beautifulsoup4>=4.12
flask>=3.0
flask-cors>=4.0

# The Selenium fallback, used when SSSB's vacancy list can't be read over
# plain HTTP. Needs Chrome/Chromium installed.
selenium>=4.20
webdriver-manager>=4.0

# Desktop notifications, and OS-keychain credential storage for the
# --with-login fallback. Both imported lazily, so neither is load-bearing.
plyer>=2.1
keyring>=24.0

# Reading a certificate's Authority Information Access extension, so a server
# that omits its intermediate certificate can still be reached — afbostader.se
# does exactly that, which is why Lund published nothing. Listed explicitly
# rather than leant on as a transitive dependency: on Linux it arrives via
# keyring -> SecretStorage, but macOS keyring uses the Keychain backend and
# pulls in no crypto at all, so on Ivar's Mac it would simply have been absent.
cryptography>=42.0
